name: 📢 Daily Social Post + Deploy

on:
  schedule:
    - cron: '0 14 * * *'   # 14:00 UTC = 9:00 AM CDT (summer) / 8:00 AM CST (winter)
  workflow_dispatch:
    inputs:
      dry_run:
        description: "DRY RUN (default: OFF — manual dispatch posts for real). Toggle ON to compose + upload banners without posting."
        required: false
        type: boolean
        default: false

jobs:
  post-and-deploy:
    name: 4 IG posts → build → rsync to Hostinger
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write

    env:
      HOSTINGER_HOST: ${{ secrets.HOSTINGER_HOST }}
      HOSTINGER_USER: ${{ secrets.HOSTINGER_USER }}
      HOSTINGER_PORT: ${{ secrets.HOSTINGER_PORT }}
      HOSTINGER_DEST: domains/hotproductsdot.com/public_html/

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: site/package-lock.json

      - name: Install site dependencies
        working-directory: site
        run: npm ci

      # Cache Next.js's incremental build artifacts (.next/cache) so subsequent
      # builds reuse webpack module hashes, RSC payloads, and image-optimization
      # output. The cache key is keyed on the lockfile + every .ts/.tsx/.js
      # source file in site/, so any source change busts the cache and triggers
      # a clean rebuild.
      - name: Restore Next.js build cache
        uses: actions/cache@v4
        with:
          path: site/.next/cache
          key: nextjs-${{ runner.os }}-${{ hashFiles('site/package-lock.json') }}-${{ hashFiles('site/**/*.ts', 'site/**/*.tsx', 'site/**/*.js', 'site/**/*.mjs') }}
          restore-keys: |
            nextjs-${{ runner.os }}-${{ hashFiles('site/package-lock.json') }}-
            nextjs-${{ runner.os }}-

      # Defense vs re-run double posts: when a workflow run is re-run
      # (run_attempt > 1), GitHub re-checks-out the ORIGINAL commit. If
      # attempt 1 successfully pushed log entries (step 9) before failing
      # downstream (e.g. SSH/rsync), attempt 2's checkout is stale —
      # post_daily.py's dedup misses attempt 1's posts and re-picks 4 new
      # products. Pulling the latest post_log.csv from origin/main here
      # closes the gap. post_daily.py also has a DAILY_PLATFORM_QUOTA
      # guard as a second line of defense.
      - name: Sync post log from origin/main (anti double-post)
        run: |
          set -e
          git fetch --quiet origin main
          git checkout origin/main -- marketing-campaigns/post_log.csv
          echo "post_log.csv synced to origin/main HEAD ($(git rev-parse --short origin/main))"
          wc -l marketing-campaigns/post_log.csv

      - name: Post 4 products to Instagram
        env:
          # AI vision gate enforces semantic banner quality (scene clutter,
          # brand mismatch, weird crops, anything heuristics can't measure).
          # Verified locally on 5 banners — flip to "shadow" to log without
          # blocking, "off" to skip the API call entirely.
          BANNER_AI_GATE: enforce
          # Posting policy:
          #   - schedule:          REAL POSTING (production daily cron).
          #   - workflow_dispatch: honors the dry_run checkbox (default ON,
          #                        so manual runs stay safe-by-default).
          # Validators (BANNER_AI_GATE=enforce + --catalog-image-only) gate
          # bad banners regardless of trigger.
          #
          # The decision lives in bash, not a GHA expression: an earlier
          # `(cond) && '' || '--dry-run'` form silently always returned
          # '--dry-run' because empty string is falsy in GHA, cascading
          # through the ||. That bug forced every dispatch into dry-run
          # regardless of the input checkbox.
          EVENT_NAME:    ${{ github.event_name }}
          DRY_RUN_INPUT: ${{ inputs.dry_run }}
          IG_USER_ID:          ${{ secrets.IG_USER_ID }}
          IG_ACCESS_TOKEN:     ${{ secrets.IG_ACCESS_TOKEN }}
          TIKTOK_ACCESS_TOKEN: ${{ secrets.TIKTOK_ACCESS_TOKEN }}
          # Cloudinary is the sole image host for Instagram banners; ImgBB was
          # dropped (commits 82cf6ab0, 7dbbb8d1) because Meta blocks i.ibb.co.
          CLOUDINARY_URL:      ${{ secrets.CLOUDINARY_URL }}
          GEMINI_API_KEY:      ${{ secrets.GEMINI_API_KEY }}
          # TAVILY grounds the --ad-creative banner in real-world reference
          # images. Missing key only degrades quality (Gemini still runs
          # without references) — the pipeline does not error out.
          TAVILY_API_KEY:      ${{ secrets.TAVILY_API_KEY }}
          ANTHROPIC_API_KEY:   ${{ secrets.ANTHROPIC_API_KEY }}
          MODELSLAB_KEY:       ${{ secrets.MODELSLAB_KEY }}
        run: |
          if [ "$EVENT_NAME" = "schedule" ]; then
            DRY_RUN_FLAG=""
            echo "Mode: REAL POSTING (scheduled cron)"
          elif [ "$EVENT_NAME" = "workflow_dispatch" ] && [ "$DRY_RUN_INPUT" = "false" ]; then
            DRY_RUN_FLAG=""
            echo "Mode: REAL POSTING (manual dispatch with dry_run=false)"
          else
            DRY_RUN_FLAG="--dry-run"
            echo "Mode: DRY RUN (event=$EVENT_NAME dry_run_input=$DRY_RUN_INPUT)"
          fi

          # Exit-code contract from post_daily.py:
          #   0 = at least one platform posted successfully
          #   1 = at least one platform attempted and failed
          #   2 = banner quality_blocked (product quarantined, no post)
          #   3 = no platform attempted (dry-run or operator skipped approval)
          # Counting only rc=0 as "posted" prevents dry-run iterations from
          # masquerading as successful posts (the old `if python ...; then ok++`
          # form treated rc=0 from a skip as a real post).
          posts_ok=0
          posts_skipped=0
          posts_blocked=0
          # Dry-run short-circuit: post_daily.py still runs pre_post_refresh,
          # ad-creative generation (Tavily + Gemini), and banner validation
          # before the dry-run gate, burning ~$ in API credits per iteration.
          # Skip the loop entirely — counters stay at 0 and every downstream
          # gate (real-post check, dup-check, log-commit, deploy) is already
          # guarded by `[ -z "$DRY_RUN_FLAG" ]` or `!inputs.dry_run`.
          if [ -n "$DRY_RUN_FLAG" ]; then
            echo "::notice::Dry-run mode — skipping 4-post Instagram loop (no API calls, no credit spend)"
          else
            for i in 1 2 3 4; do
              echo "::group::Post $i/4"
              # --ad-creative: AI-generated ad-creative banner (Gemini Nano Banana
              # Pro grounded on Tavily reference imagery). The white-card pipeline
              # replaced rembg cutouts, so the historical reason for forcing
              # --catalog-image-only is gone. Stochastic flakiness now lands in
              # quality_blocked (rc=2) rather than corrupting banners.
              python post_daily.py --platform instagram --ad-creative && rc=0 || rc=$?
              case "$rc" in
                0) posts_ok=$((posts_ok + 1)); echo "Post $i: posted (rc=0)" ;;
                2) posts_blocked=$((posts_blocked + 1)); echo "::notice::Post $i quality_blocked (rc=2)" ;;
                3) posts_skipped=$((posts_skipped + 1)); echo "::notice::Post $i skipped (rc=3, unapproved)" ;;
                *) echo "::warning::Post $i failed (rc=$rc, continuing)" ;;
              esac
              echo "::endgroup::"
            done
          fi
          echo "Posts: ${posts_ok} posted, ${posts_skipped} skipped, ${posts_blocked} blocked (of 4)"
          # Real-posting runs require at least one true post UNLESS every
          # invocation skipped (rc=3) — that means post_daily.py's daily-quota
          # guard fired because the day's posts were already made by a
          # previous (succeeded) attempt. In that case the redeploy still
          # needs to run, so don't fail the workflow.
          if [ -z "$DRY_RUN_FLAG" ] && [ "$posts_ok" -eq 0 ] && [ "$posts_skipped" -ne 4 ]; then
            echo "::error::Real-posting run produced 0 successful posts — aborting deploy"
            exit 1
          fi

      # TikTok: 1 post per day, non-fatal. Runs after Instagram so the rotation
      # pool is already pruned of today's IG picks — TikTok gets a fresh product.
      # continue-on-error: true ensures a TikTok API failure never blocks the
      # site deploy or the IG log commit.
      - name: Post 1 product to TikTok
        continue-on-error: true
        env:
          BANNER_AI_GATE: enforce
          EVENT_NAME:          ${{ github.event_name }}
          DRY_RUN_INPUT:       ${{ inputs.dry_run }}
          TIKTOK_ACCESS_TOKEN: ${{ secrets.TIKTOK_ACCESS_TOKEN }}
          CLOUDINARY_URL:      ${{ secrets.CLOUDINARY_URL }}
          GEMINI_API_KEY:      ${{ secrets.GEMINI_API_KEY }}
          TAVILY_API_KEY:      ${{ secrets.TAVILY_API_KEY }}
          ANTHROPIC_API_KEY:   ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [ "$EVENT_NAME" = "schedule" ]; then
            DRY_RUN_FLAG=""
            echo "TikTok: REAL POSTING (scheduled cron)"
          elif [ "$EVENT_NAME" = "workflow_dispatch" ] && [ "$DRY_RUN_INPUT" = "false" ]; then
            DRY_RUN_FLAG=""
            echo "TikTok: REAL POSTING (manual dispatch)"
          else
            echo "TikTok: dry-run mode — skipping"
            exit 0
          fi
          python post_daily.py --platform tiktok --ad-creative $DRY_RUN_FLAG \
            && echo "TikTok: posted OK" \
            || echo "::warning::TikTok post failed (non-fatal — IG deploy continues)"

      # Defense-in-depth: catch any duplicate IG posts written today before the
      # log gets pushed to origin/main. Triggered when the rotation fallback
      # path mis-fires (the 2026-05-04 triple-Zeiss incident — pick_next_product
      # returned products[(day_of_year-1) % 60] deterministically when the
      # 60-product pool was exhausted by `posted`, so iterations 2/3/4 all
      # picked the same product). Fix landed in 6acb6e6f, but if a future
      # regression slips a duplicate through, fail loud here so the operator
      # sees an error rather than discovering it on the IG feed days later.
      - name: Verify no duplicate IG posts today
        if: success() && (github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && !inputs.dry_run))
        run: |
          today="$(date -u +%F)"
          dups=$(awk -F, -v d="$today" '$1==d && $2=="instagram" && $4=="ok" {print $3}' \
            marketing-campaigns/post_log.csv | sort | uniq -d)
          if [ -n "$dups" ]; then
            echo "::error::Duplicate Instagram posts detected for ${today}:"
            echo "$dups" | sed 's/^/  - /'
            echo "::error::IG side has the duplicates already; log push blocked to surface the bug."
            exit 1
          fi
          echo "OK: ${today} has no duplicate IG posts."

      # Commit the post log immediately after posting succeeds, BEFORE build/deploy.
      # If we deferred this until after rsync (the previous behavior), any deploy
      # failure would orphan the day's post records — the next run's dedup logic
      # would then re-pick the same products (e.g. DJI Air 3S re-selected on
      # consecutive days because yesterday's deploy hiccup blocked the log push).
      - name: Commit post log (pre-deploy)
        # Match the same gate as DRY_RUN_FLAG: commit log entries whenever
        # this run actually posted — schedule (always real) or
        # workflow_dispatch with dry_run=false. Dry-run dispatches don't
        # produce log changes anyway, but keeping the condition consistent
        # avoids future drift.
        if: success() && (github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && !inputs.dry_run))
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add marketing-campaigns/post_log.csv
          git diff --staged --quiet || (
            git commit -m "chore: daily rotation $(date -u +%F) [skip ci]" &&
            git pull --rebase --autostash origin main &&
            git push
          )

      - name: Build static site
        working-directory: site
        run: npm run build

      - name: Set up SSH for Hostinger
        env:
          HOSTINGER_SSH_KEY_B64: ${{ secrets.HOSTINGER_SSH_KEY_B64 }}
          HOSTINGER_KNOWN_HOSTS: ${{ secrets.HOSTINGER_KNOWN_HOSTS }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh

          # 1. Verify secret is present (without leaking it)
          if [ -z "${HOSTINGER_SSH_KEY_B64:-}" ]; then
            echo "::error::HOSTINGER_SSH_KEY_B64 secret is empty or unset"
            exit 1
          fi
          echo "Secret length (base64 chars): ${#HOSTINGER_SSH_KEY_B64}"

          # 2. Decode. Use printf (not echo) to avoid trailing newline mismatch
          # and pipe through tr -d to strip any whitespace/CR that may have crept
          # into the secret value via the GitHub UI.
          printf '%s' "$HOSTINGER_SSH_KEY_B64" | tr -d '[:space:]' | base64 -d > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

          # 3. Diagnostics on the decoded key (safe — only metadata)
          KEY_BYTES=$(wc -c < ~/.ssh/id_rsa)
          echo "Decoded key bytes: $KEY_BYTES"
          if [ "$KEY_BYTES" -lt 100 ]; then
            echo "::error::Decoded key suspiciously small — likely bad base64"
            exit 1
          fi
          if grep -q $'\r' ~/.ssh/id_rsa; then
            echo "::warning::Key contains CR characters — stripping"
            tr -d '\r' < ~/.ssh/id_rsa > ~/.ssh/id_rsa.clean
            mv ~/.ssh/id_rsa.clean ~/.ssh/id_rsa
            chmod 600 ~/.ssh/id_rsa
          fi
          echo "Key first line: $(head -1 ~/.ssh/id_rsa)"
          echo "Key last line:  $(tail -1 ~/.ssh/id_rsa)"

          # 4. Parse-check (don't silence stderr — we want the real error)
          if ! ssh-keygen -y -f ~/.ssh/id_rsa > /dev/null; then
            echo "::error::ssh-keygen rejected the key. Re-encode locally with: base64 -w0 < ~/.ssh/hostinger_key | pbcopy"
            exit 1
          fi
          echo "Key parses OK"

          # 5. Seed known_hosts from the HOSTINGER_KNOWN_HOSTS secret. We do NOT
          # run ssh-keyscan from the runner: Hostinger silently drops
          # unauthenticated banner probes from CI runner IP ranges (observed
          # 2026-05-07: 5/5 keyscan attempts returned empty stdout AND stderr
          # while the same runner could complete an authenticated SSH session
          # moments later). Storing the fingerprints in a secret lets us run
          # StrictHostKeyChecking=yes for real verification — no TOFU window.
          if [ -z "${HOSTINGER_KNOWN_HOSTS:-}" ]; then
            echo "::error::HOSTINGER_KNOWN_HOSTS secret is empty or unset. Run 'ssh-keyscan -p <port> <host>' from a trusted machine and store the output as the HOSTINGER_KNOWN_HOSTS GitHub secret."
            exit 1
          fi
          printf '%s\n' "$HOSTINGER_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts
          # Sanity check: at least one non-comment, non-blank entry must exist.
          if ! grep -Eq '^[^#[:space:]]' ~/.ssh/known_hosts; then
            echo "::error::HOSTINGER_KNOWN_HOSTS contains only comments/whitespace — no usable host keys"
            exit 1
          fi
          echo "known_hosts entries (non-comment): $(grep -Ec '^[^#[:space:]]' ~/.ssh/known_hosts)"

          # Connection multiplexing — the smoke test opens a master SSH
          # channel and the subsequent rsync reuses it, skipping a second
          # full TCP+TLS+auth handshake to Hostinger. The handshake is the
          # dominant flakiness source on this host (fail2ban warm-up, slow
          # PAM), so reusing it cuts the deploy's failure surface roughly
          # in half. ControlPersist=10m keeps the master alive long enough
          # to cover post + build + rsync even on slow runs.
          cat >> ~/.ssh/config <<'EOF'
          Host *
              ControlMaster auto
              ControlPath ~/.ssh/cm-%r@%h:%p
              ControlPersist 10m
          EOF
          chmod 600 ~/.ssh/config

      - name: SSH connectivity smoke test
        # Hostinger shared hosting is intermittently slow on the SSH handshake
        # (fail2ban warm-up, occasional load spikes). The previous 10s
        # ConnectTimeout + zero retries was the dominant cause of "intermittent
        # SSH failure" workflow runs. Bump to 30s, keep BatchMode (no prompts),
        # add ConnectionAttempts and keepalives, and retry up to 4 times with
        # exponential-ish backoff.
        run: |
          set -e
          attempt=1
          max_attempts=4
          while : ; do
            if ssh -p "$HOSTINGER_PORT" -i ~/.ssh/id_rsa \
                -o BatchMode=yes \
                -o ConnectTimeout=30 \
                -o ConnectionAttempts=3 \
                -o ServerAliveInterval=20 \
                -o ServerAliveCountMax=6 \
                -o StrictHostKeyChecking=yes \
                -o UserKnownHostsFile=~/.ssh/known_hosts \
                "$HOSTINGER_USER@$HOSTINGER_HOST" \
                "echo OK from \$(hostname) && pwd && ls -ld $HOSTINGER_DEST"; then
              echo "Smoke test succeeded on attempt $attempt"
              break
            fi
            if [ "$attempt" -ge "$max_attempts" ]; then
              echo "::error::SSH smoke test failed after $max_attempts attempts"
              exit 1
            fi
            wait_s=$((attempt * 15))
            echo "::warning::SSH smoke test attempt $attempt failed; retrying in ${wait_s}s"
            sleep "$wait_s"
            attempt=$((attempt + 1))
          done

      - name: Rsync to Hostinger
        # --delete removes anything in dest not present in source. The
        # exclude list below preserves files Hostinger generates server-side
        # that the static export doesn't (and shouldn't) ship — losing any
        # of these would break SSL renewal, panel-managed caching, or PHP
        # behavior. site/out/ contains none of these patterns, so they
        # cannot legitimately be re-introduced from source.
        #
        # Resilience flags (added 2026-05-05 to fix intermittent SSH failures):
        #   --partial          : preserve partial transfers so a retry resumes
        #                        instead of restarting from zero.
        #   --timeout=60       : abort if no I/O for 60s (catches frozen TCP
        #                        connections faster than the kernel default).
        #   ssh -o ServerAlive*: keep the channel alive across Hostinger's
        #                        idle-cutoff and brief network blips.
        #   Retry loop         : 3 attempts with backoff. Most rsync drops on
        #                        Hostinger are single-event; a retry usually
        #                        completes in seconds because --partial means
        #                        only the unfinished delta moves.
        run: |
          set -e
          attempt=1
          max_attempts=3
          while : ; do
            if rsync -avz --delete --partial --timeout=60 \
                --exclude='.htaccess' \
                --exclude='.htpasswd' \
                --exclude='.well-known/' \
                --exclude='cgi-bin/' \
                --exclude='.user.ini' \
                --exclude='error_log' \
                --exclude='error_log.*' \
                --exclude='php.ini' \
                -e "ssh -p $HOSTINGER_PORT -i ~/.ssh/id_rsa -o BatchMode=yes -o ConnectTimeout=30 -o ConnectionAttempts=3 -o ServerAliveInterval=20 -o ServerAliveCountMax=6 -o StrictHostKeyChecking=yes -o UserKnownHostsFile=$HOME/.ssh/known_hosts" \
                site/out/ \
                "$HOSTINGER_USER@$HOSTINGER_HOST:$HOSTINGER_DEST"; then
              echo "Rsync succeeded on attempt $attempt"
              break
            fi
            if [ "$attempt" -ge "$max_attempts" ]; then
              echo "::error::Rsync failed after $max_attempts attempts"
              exit 1
            fi
            wait_s=$((attempt * 30))
            echo "::warning::Rsync attempt $attempt failed; retrying in ${wait_s}s (--partial preserves progress)"
            sleep "$wait_s"
            attempt=$((attempt + 1))
          done

      - name: Upload composed banners as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: banners-${{ github.run_id }}
          path: generated_images/**/banner.jpg
          if-no-files-found: warn
          retention-days: 14
