# hotproductsdot-v2 — Portfolio Agent Harness

> Auto-generated by `agentic-os/harness/install.py`. Safe to edit the **Project** and **Commands** sections; re-run install with `--merge` to refresh shared rules.

## Project

- **Archetype:** fullstack-mixed
- **Path (WSL):** `/mnt/e/GITHUB/hotproductsdot-v2`
- **Path (Windows):** `E:\GITHUB\hotproductsdot-v2`
- **Notes:** Affiliate site + growth-engine + agentic-os control plane

## Portfolio context

This repo is part of a shared portfolio managed by **Agentic OS** (`hotproductsdot-v2/agentic-os`).

| Tool | Role |
|------|------|
| **Cursor / Claude Code** | Write and review production code |
| **Hermes** | Orchestrate cron, research, cross-project synthesis |
| **Mission Control** | Project registry + Pantheon UI → `:9120` |
| **Claude OS Bridge** | `agentic-os/bridge/context/latest.json` |
| **Obsidian** | `/mnt/e/GITHUB/Claude-Code-OBVault` |

### Pantheon delegation

| Persona | Use for |
|---------|---------|
| **Mercury** | Status, cron, briefs, routine checks |
| **Labyrinth** | Research, SEO, competitive analysis |
| **Philosopher** | Strategy, architecture, trade-offs |
| **Oracle** | "What did I work on?" — reads bridge + vault |

Say `use Mercury/Labyrinth/Philosopher/Oracle` or invoke via Hermes `/personality`.

## Global rules (all portfolio repos)

- Secrets live in repo-root .env only — never commit credentials
- Never force-push to main/master; confirm before destructive git operations
- Delegate production code changes to Cursor or Claude Code when available
- Hermes orchestrates; it does not rewrite app code unless explicitly asked
- Use --dry-run for publish/deploy pipelines unless the user explicitly approves
- WSL paths for Hermes/cron; Windows paths for Cursor — both are valid
- Cross-project context lives in agentic-os/bridge/context/latest.json (Oracle reads this)
- Obsidian vault is shared PKM across the portfolio

## Commands

```bash
cd site && npm start
```

```bash
cd site && npm install && npm run build
```

**Python entrypoints:** `requirements.txt`, `growth-engine/scripts/run_daily.py`, `agentic-os/mission-control/server.py`, `pipeline-ui/server.py`

## Verification before hand-off

```bash
npm run build
# npm test — if defined
```

### Stack notes

- **Site:** Next.js under `site/` (static export or Vercel)
- **Automation:** Python under `growth-engine/` or repo-root scripts
- **Control plane:** `agentic-os/` when present (Pantheon, bridge, Mission Control)

### Project-specific rules

- Run growth/automation with `--dry-run` unless the user explicitly approves publish/deploy
- Product images and CSVs are data assets — do not regenerate in bulk without approval
- Generated guides live in `site/content/guides-generated/` — match existing JSON schema
