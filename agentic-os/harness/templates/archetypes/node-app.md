### Stack notes

- Standalone Node project (Express, Vite, React, or tooling)

### Project-specific rules

- Use the package manager already in the repo (`package-lock.json` → npm, `pnpm-lock.yaml` → pnpm)
- Run `npm run build` and `npm test` (if defined) before claiming done
